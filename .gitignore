################################################################################
# This .gitignore file was automatically created by Microsoft(R) Visual Studio.
################################################################################

/.claude
/reports
/.vs
/data

# Credentials folder — secrets and tenant configs must never be committed.
# The README.md and .gitkeep inside are tracked; everything else is not.
/credentials/*
!/credentials/README.md
!/credentials/.gitkeep
